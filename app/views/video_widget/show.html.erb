<div class="dynamic-container">
  <video class="projekktor" width="100%" height="100%" poster="<%= widget.poster && cms_url(widget.poster) %>" controls="controls">
    <source src="<%= widget.source && cms_url(widget.source) %>" type="<%= widget.mime_type %>" />
  </video>
</div>
