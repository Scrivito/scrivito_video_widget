<div class="dynamic-container">
  <video class="projekktor" width="100%" height="100%" poster="<%= widget.poster && scrivito_url(widget.poster) %>" controls="controls">
    <source src="<%= widget.source.try(:binary_url) %>" type="<%= widget.mime_type %>" />
  </video>
</div>
